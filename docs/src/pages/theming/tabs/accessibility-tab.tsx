import { component$ } from '@qwik.dev/core';

interface AccessibilityTabProps {
  shadeVars: Record<string, string>;
}

export const AccessibilityTab = component$<AccessibilityTabProps>(({ shadeVars }) => {
  return <></>;
});
