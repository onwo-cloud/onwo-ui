import { component$ } from '@qwik.dev/core';

interface TypographyTabProps {
  shadeVars: Record<string, string>;
}

export const TypographyTab = component$<TypographyTabProps>(({ shadeVars }) => {
  return <></>;
});
